if [[ ! -f ~/.zpm/zpm.zsh ]]; then
	git clone --recursive https://github.com/horosgrisa/ZPM ~/.zpm
fi

source ~/.zpm/zpm.zsh

Plug nvm golang horosgrisa/zsh-gvm  #Package managers & langs

Plug datauri ack dropbox myip extract pass tor tmux genpasswd viewer desktop hosts command-not-found rmate  #Programs

Plug colors grc dircolors  #Colors

Plug human zsh-core ls cd  #ZSH

Plug battery node status path git  #Prompt

Plug horosgrisa/zsh-marks horosgrisa/zsh-completions voronkovich/gitignore.plugin.zsh zsh-users/zsh-syntax-highlighting horosgrisa/zsh-history-substring-search horosgrisa/zsh-autosuggestions horosgrisa/autoenv horosgrisa/fasd  horosgrisa/mysql-colorize

plugins=( lol )

Plug oh-my-zsh-wrapper

# Variables

PROMPT='%{$fg[magenta]%} %M %{$fg[yellow]%}➜ %{$reset_color%}'
RPROMPT=' $last_status$node_version$gitprompt$battery_charge$current_path%{$reset_color%}'
PS2="%{$fg[green]%}%_>%{$reset_color%}"
PROMPT_EOL_MARK="%{$bg[cyan]%}%{$fg[white]%}⏎%{$reset_color%}"
WORDCHARS='*?_-~=&;!#$%^()[]{}<>:.'

CURRENT_PATH_PREFIX=" "
GIT_PREFIX=" "
NODE_VERSION_PREFIX=" "
BATTERY_CHARGE_PREFIX=" "

ZSH_HIGHLIGHT_HIGHLIGHTERS=(main brackets pattern line)

# Aliaces

alias f='fasd -f -e vim'
alias o='fasd -e xdg-open'
alias d='fasd_cd -d -i'
alias j='fasd_cd -d -i'
alias \$=""
