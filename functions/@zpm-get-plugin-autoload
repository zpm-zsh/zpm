#!/usr/bin/env zsh

local plugin_spec="$1"
local autoload_files="$2"

if [[ "$plugin_spec" == *",autoload:"* ]]; then
  local autoload_suffix="${plugin_spec##*,autoload}"
  autoload_suffix="${autoload_suffix%%,*}"
  autoload_files+="$autoload_suffix"
fi

print -r -- "$autoload_files"
